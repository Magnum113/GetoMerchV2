# 🚀 Быстрый запуск GetoMerch

## Способ 1: Двойной клик (macOS)

1. Найдите файл `start.command` в корне проекта
2. Двойной клик по файлу
3. Проект запустится автоматически и откроет браузер на странице Operations

> **Примечание**: Если файл не запускается, выполните в терминале:
> ```bash
> chmod +x start.command
> ```

## Способ 2: Через терминал

### Быстрый запуск:
```bash
# Просто запустите скрипт
./start.sh

# Или если нет прав на выполнение:
chmod +x start.sh && ./start.sh
```

### Или используйте npm/pnpm:
```bash
# Установите зависимости (только первый раз)
npm install
# или
pnpm install

# Запустите проект
npm run dev
# или
pnpm run dev

# Откройте в браузере: http://localhost:3000/operations
```

## Способ 3: Через npm скрипт с автозапуском браузера

```bash
npm run dev:open
```

Этот скрипт запустит сервер и автоматически откроет страницу Operations в браузере.

## 🔧 Требования

- **Node.js** версии 18+ (рекомендуется через Homebrew)
- **npm** или **pnpm** для управления зависимостями

### Установка Node.js (если не установлен):

```bash
# Через Homebrew (рекомендуется для macOS)
brew install node

# Или скачайте с официального сайта
# https://nodejs.org/
```

## 📍 Основные страницы

После запуска проекта доступны следующие страницы:

- **Операционный центр**: http://localhost:3000/operations
- **Панель управления**: http://localhost:3000/dashboard
- **Каталог товаров**: http://localhost:3000/catalog
- **Заказы**: http://localhost:3000/orders
- **Производство**: http://localhost:3000/production
- **Инвентарь**: http://localhost:3000/inventory
- **Аналитика**: http://localhost:3000/analytics

## 🛑 Остановка сервера

Нажмите `Ctrl + C` в терминале, где запущен сервер.

Или используйте команду:
```bash
pkill -f "next dev"
```

## 🔄 Перезапуск

Если что-то пошло не так, используйте команду перезапуска:

```bash
npm run restart
```

Или вручную:
```bash
# Остановите сервер
pkill -f "next dev"

# Очистите кэш (опционально)
npm run clean

# Запустите снова
npm run dev
```

## ⚙️ Настройка переменных окружения

Убедитесь, что файл `.env` существует и содержит:

```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

Без этих переменных проект не сможет подключиться к базе данных.

## 🐛 Решение проблем

### Проблема: "Node.js не найден"
**Решение**: Установите Node.js через Homebrew:
```bash
brew install node
```

### Проблема: "Port 3000 уже занят"
**Решение**: Остановите другие процессы на порту 3000:
```bash
lsof -ti:3000 | xargs kill -9
```

### Проблема: Ошибки Turbopack
**Решение**: Очистите кэш и перезапустите:
```bash
npm run clean
npm run dev
```

### Проблема: Зависимости не установлены
**Решение**: Установите зависимости:
```bash
rm -rf node_modules package-lock.json
npm install
```

## 📝 Полезные команды

```bash
# Установка зависимостей
npm install

# Запуск dev-сервера
npm run dev

# Сборка production версии
npm run build

# Запуск production версии
npm start

# Очистка кэша сборки
npm run clean

# Перезапуск сервера
npm run restart

# Проверка кода (linting)
npm run lint
```

## 🎯 Рекомендуемый workflow

1. **Первый запуск**:
   ```bash
   npm install
   ./start.sh
   ```

2. **Ежедневная работа**:
   ```bash
   # Просто двойной клик на start.command
   # или
   npm run dev
   ```

3. **После изменений в зависимостях**:
   ```bash
   npm install
   npm run clean
   npm run dev
   ```

---

**Готово!** Теперь вы можете быстро запускать проект одним кликом! 🎉

