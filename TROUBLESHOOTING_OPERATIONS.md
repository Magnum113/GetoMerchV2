# 🔧 Решение проблем с Operations

## Проблема: Заказы из Ozon не отображаются в Operations

### Шаг 1: Синхронизация заказов

1. Перейдите на страницу **Заказы** (`/orders`)
2. Нажмите кнопку **"Sync Orders"** (Синхронизировать заказы)
3. Дождитесь завершения синхронизации

После синхронизации операционные статусы пересчитываются автоматически.

### Шаг 2: Пересчет операционных статусов

Если заказы синхронизированы, но не отображаются правильно:

1. Перейдите на страницу **Operations** (`/operations`)
2. Нажмите кнопку **"Пересчитать статусы"**
3. Дождитесь завершения пересчета

### Шаг 3: Проверка данных

#### Проверка заказов в базе:

```sql
-- Проверяем последние заказы
SELECT 
  order_number,
  operational_status,
  warehouse_type,
  created_at
FROM orders
ORDER BY created_at DESC
LIMIT 10;
```

#### Проверка товаров в заказах:

```sql
-- Проверяем товары в заказах
SELECT 
  o.order_number,
  o.operational_status,
  oi.fulfillment_type,
  oi.fulfillment_status,
  p.name as product_name,
  oi.quantity
FROM orders o
JOIN order_items oi ON o.id = oi.order_id
JOIN products p ON oi.product_id = p.id
WHERE o.operational_status IN ('WAITING_FOR_PRODUCTION', 'WAITING_FOR_MATERIALS')
ORDER BY o.created_at DESC;
```

### Шаг 4: Проверка рецептов и материалов

Для того, чтобы система могла определить дефицит материалов, нужно:

1. **Создать рецепты для товаров** (если их нет):
   - Перейдите в раздел **Производство**
   - Добавьте рецепты для товаров, которые нужно производить

2. **Проверить наличие материалов**:
   - Перейдите в раздел **Материалы**
   - Убедитесь, что материалы добавлены и имеют правильные остатки

3. **Проверить связь материалов с рецептами**:
   ```sql
   -- Проверяем рецепты и материалы
   SELECT 
     p.name as product_name,
     r.id as recipe_id,
     COUNT(rm.id) as materials_count
   FROM products p
   LEFT JOIN recipes r ON p.id = r.product_id
   LEFT JOIN recipe_materials rm ON r.id = rm.recipe_id
   GROUP BY p.id, p.name, r.id;
   ```

### Шаг 5: Проверка логики fulfillment

Система автоматически определяет сценарий исполнения заказа:

- **READY_STOCK** - товар есть на складе → статус `READY_TO_SHIP`
- **PRODUCE_ON_DEMAND** - нужно произвести → статус `WAITING_FOR_PRODUCTION` или `WAITING_FOR_MATERIALS`
- **FBO** - исполняет Ozon → статус `PENDING`

Проверьте, правильно ли определены сценарии:

```sql
-- Проверяем fulfillment типы
SELECT 
  fulfillment_type,
  fulfillment_status,
  COUNT(*) as count
FROM order_items
GROUP BY fulfillment_type, fulfillment_status;
```

## Что было исправлено

1. ✅ **Подключен расчет дефицита материалов** - теперь блоки "Чего не хватает" и "Заявка на пополнение" работают
2. ✅ **Улучшена логика расчета дефицита** - теперь проверяются все заказы, требующие производства, а не только operation_tasks
3. ✅ **Автоматический пересчет статусов** - после синхронизации заказов статусы пересчитываются автоматически

## Типичные проблемы

### Проблема: Заказы не синхронизируются

**Решение:**
- Проверьте настройки Ozon API в разделе **Настройки**
- Убедитесь, что API ключи правильные
- Проверьте логи в консоли браузера (F12)

### Проблема: Заказы синхронизированы, но не отображаются в Operations

**Решение:**
1. Нажмите кнопку **"Пересчитать статусы"** на странице Operations
2. Проверьте, что заказы имеют правильный `warehouse_type` (должен быть `FBS`)
3. Проверьте, что товары в заказах существуют в базе данных

### Проблема: Дефицит материалов не показывается

**Решение:**
1. Убедитесь, что для товаров созданы рецепты
2. Убедитесь, что материалы добавлены в систему
3. Проверьте, что материалы связаны с рецептами через `recipe_materials`
4. Проверьте остатки материалов в разделе **Материалы**

### Проблема: Заказы показываются как "Нужно произвести", но материалов достаточно

**Решение:**
- Это может быть из-за того, что операционный статус не пересчитан
- Нажмите кнопку **"Пересчитать статусы"**
- Проверьте, что материалы правильно связаны с рецептами

## Логи для отладки

Откройте консоль браузера (F12) и проверьте логи с префиксом `[v0]`:

- `[v0] Загружаю данные операций...` - загрузка данных на странице Operations
- `[v0] Запрос заказов с operational_status = WAITING_FOR_PRODUCTION...` - запрос заказов для производства
- `[v0] OperationsService: Анализирую дефицит материалов...` - расчет дефицита материалов

## Контакты для поддержки

Если проблема не решена, проверьте:
1. Логи в консоли браузера
2. Логи сервера (в терминале, где запущен `npm run dev`)
3. Данные в базе данных через SQL запросы выше

